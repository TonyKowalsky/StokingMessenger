# Messenger Demo Prototype

Это демонстрационная версия окна чата мессенджера, созданная с использованием React, Redux Toolkit, RTK Query. Данное приложение доступно для просмотра по адресу https://stoking-messenger.vercel.app/

> **Важно:** Данный проект является **MVP (Minimum Viable Product)** с минимальным набором функций. Основной акцент сделан на чистоту кода, масштабируемость архитектуры и стабильность базовых механик.

## 🚀 Основной стек

- **React 19** + **TypeScript**
- **Redux Toolkit** (State Management)
- **Vite** (Build Tool)
- **CSS Modules** (Styling)
- **RTK Query** (Работа с Giphy API)

## ✨ Текущие возможности (Demo)

- **Текстовые сообщения:** Отправка и отображение сообщений в реальном времени.
- **Интеграция Giphy:** Поиск и отправка GIF-анимаций через официальное API.
- **Emoji Picker:** Компактная панель для выбора и отправки эмодзи.
- **Работа с файлами:** Механика выбора и отображения вложений (имитация загрузки).
- **Persistence:** Сохранение истории переписки в LocalStorage (данные не пропадают после перезагрузки).
- **Умная группировка:** Автоматическое разделение сообщений по дням с центрированными заголовками дат.
- **Эмуляция чат-бота:** Middleware, который имитирует ответы собеседника.

## 🏗 Архитектура (FSD)

Проект строго следует методологии **Feature-Sliced Design**:

- **Shared:** Переиспользуемые UI-компоненты (IconButton, Avatar), утилиты и API.
- **Entities:** Бизнес-сущности[Message и User с изолированной логикой.
- **Features:** Независимые части функционала (EmojiPicker, GifPicker, MessageInput).
- **Widgets:** Крупные блоки страницы, такие как Chat, которые собирают фичи и сущности воедино.

### Архитектурные особенности:

- **Decoupling:** Фичи не импортируют друг друга. Взаимодействие реализовано через паттерн _Render Props_ на уровне виджетов.
- **Public API:** Каждый модуль предоставляет строго определенный интерфейс через index.ts.
- **Cross-stack Imports:** Связи между сущностями (например, автор сообщения) реализованы через `@x` границы для предотвращения циклической зависимости.

## 🛠 Установка и запуск

1.  **Клонируйте репозиторий:**

    ```bash
    git clone https://github.com/TonyKowalsky/StokingMessenger.git
    cd StokingMessenger
    ```

2.  **Установите зависимости:**

    ```bash
    npm install
    ```

3.  **Настройте окружение:**
    Создайте файл `.env` в корневом каталоге и добавьте ваш ключ Giphy API:

    ```env
    VITE_GIPHY_API_KEY=your_api_key_here
    ```

4.  **Запустите проект:**
    ```bash
    npm run dev
    ```
